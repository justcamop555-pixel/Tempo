## Tempo 1.0.209

**Cleanup pass: removed dead caption settings and fixed stale code comments - no behavior change**
- Removed two unused settings (CaptionDriveWindows, CaptionMirrorWindows) that were saved but read nowhere - leftovers from an older caption design. Existing settings files are unaffected.
- Fixed an out-of-date description in the Windows caption reader that wrongly claimed Tempo can't transcribe on its own - it now correctly reflects that Tempo has its own offline engine with Windows as the fallback.
- Updated internal comments that still referred to the small pill removed in 1.0.208, so the code matches the normal-size caption bar that's actually shown.
- All 1.0.208 fixes (Windows bar hides immediately, normal-size Tempo bar, splash + welcome notice restored) unchanged.

Windows may show "Unknown publisher" because the app isn't code-signed - click More info → Run anyway. It's safe.
