## Tempo 1.0.234

**Quick CPS presets, and the CPS Test layout fix**
- **CPS presets.** The Manual Speed card on the Clicker tab now has one-tap buttons — **10 / 50 / 100 / 200 CPS** — so you can set the click speed (and the interval fields) instantly without typing values or dragging the slider.
- **CPS Test window** no longer overlaps at the bottom: the "Test length:" and "Button:" rows are spaced clearly and the window is a little taller, with a dark title bar on dark themes.
- Note: the destructive actions you mentioned already ask for confirmation (Clear points, Delete macro, Reset stats), and newly recorded macros are already selected automatically — so those were already covered.

Cumulative — includes the 1.0.232 translation work and everything before it.

Windows may show "Unknown publisher" because the app isn't code-signed - click More info → Run anyway. It's safe.
