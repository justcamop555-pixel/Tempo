## Tempo 1.0.233

**CPS Test window no longer overlaps at the bottom**
- In the CPS Test window the "Test length:" and "Button:" rows overlapped the results line above them and were squeezed against the bottom edge. The window is now a little taller and those selectors are laid out with clear spacing, each label vertically centred against its row of buttons.
- The CPS Test window now also uses a dark title bar on dark themes, to match the main window.

Cumulative — includes the 1.0.232 translation work and everything before it.

Windows may show "Unknown publisher" because the app isn't code-signed - click More info → Run anyway. It's safe.
